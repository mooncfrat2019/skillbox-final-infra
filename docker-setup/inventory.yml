---
all:
  hosts:
    server1:
      ansible_ssh_private_key_file: "{{ ansible_ssh_private_key_file | default('~/.ssh/id_rsa') }}"
      ansible_host: "{{ ansible_host_1a }}"
      ansible_user: "{{ ansible_user }}"
  vars:
    ansible_become: yes
    ansible_python_interpreter: /usr/bin/python3